# ML-00 — Standalone MathLab extraction

## Requested behavior

Extract the portable MathLab from `davgor/icarusUnreal` at commit `d551767cb1c3bd259bb00f56be1e52c2182c5ec3`, preserve its dependency closure and provenance, and establish a truthful standalone baseline without changing the source repository.

## Proposed mechanism

Keep the reference implementation at its original `Sim/icarus_sim` path. Place new repository packaging behind a separate `fantasy_world_generator` facade. Retain the external creature fixture, canonical MathLab docs, revision record, biome decision, historical LAB evidence, and semantic asset requirements. Verify checked-in hashes without requiring the source checkout.

## Dependencies and unresolved decisions

No implementation dependency. Public redistribution remains blocked until the owner selects an explicit source/package license. Clean-checkout CI must prove that no sibling `IcarusUnreal` checkout is needed.

## Sources consulted

`PLAN.md`; source `Sim/README.md`; MathLab docs/revision register; LAB-005/006/017/018; biome-gate decision; repository `AGENTS.md` and workflow docs.

## Files and assets in scope

`Sim/`, `tools/terrain_*`, retained docs/evidence, `provenance/`, standalone CI/packaging, contracts, and this board. No Unreal or game assets.

## Acceptance and evidence

- [x] Source commit pinned and original checkout unchanged.
- [x] Extraction manifest records source/destination SHA-256 and the one syntax-only compatibility edit.
- [x] Creature-catalogue dependency retained.
- [x] Full reference suite passes in this repository: 112 tests on Python 3.9.6.
- [x] Browser server and CLI/export smoke tests pass; loopback HTTP returned the complete MathLab page.
- [x] Wheel contains `icarus_sim`, `fantasy_world_generator`, and both packages' JSON inputs.
- [x] A full validation run passes from a temporary standalone copy with no sibling `IcarusUnreal` checkout: 112 reference tests and 5 facade tests.
- [x] Clean-checkout CI passes without the source repository present: [run 35056123735](https://github.com/davgor/FantasyWorldGenerator/actions/runs/35056123735), commit `78381cb16063c356ab362bedf02978ab0eeb757d`, Ubuntu 24.04 / Python 3.12. Validation passed for 79 provenance files, 190 reference tests and 16 facade tests; wheel, samples and artifacts also built successfully before the separate portfolio checkout.

## Documentation impact

README, docs map, publishing and Unreal-boundary documents now distinguish the extracted reference, export-only facade, and future native/plugin runtime.

## Adversarial review and limitations

Do not treat Python/JSON packaging as ML-03 runtime proof. The copied production catalogue is a semantic requirement snapshot; Unreal assets remain game-owned. Hash-manifest regeneration is restricted to the pinned source and rejects unreviewed modifications.

## Handoff

Clean-checkout CI establishes the extraction prerequisite and ML-01 may proceed. A later run at `4ea7169` failed on stale destination hashes for `terrain_climate.py` and `terrain_patch.py`; the 2026-09-16 follow-up preserves source hashes and appends reviewed corrections for those committed edits. Fresh local validation results are recorded below. No simulation or validation behavior changes; no engine evidence is claimed. No commit, push or publication was performed in this follow-up.

Follow-up validation on 2026-09-16: `python3 tools/validate_repo.py` passed on Python 3.9.6 (213 reference tests, 22 facade tests, loopback lab smoke, two identical showcase bundles and byte-reproducible asset compilation). All 79 extraction entries validate. Current clean-checkout CI must run after a separately authorized commit/push; this follow-up does not claim that the latest remote failure has been rerun.
