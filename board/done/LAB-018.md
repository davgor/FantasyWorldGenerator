# LAB-018 — Restore FantasyWorldGenerator regression suite

Status: done. Owner/session: current FantasyWorldGenerator test-cleanup session.

## Scope and acceptance

Repair regressions in the in-progress city building-plan extension without weakening existing terrain or settlement assertions. Preserve compatibility for the existing city-layout helper test, validate the building-pack schema as authored, and leave the full simulation/tooling suite green.

Expected touched files:
- `Sim/icarus_sim/terrain_settlements.py`
- `board/in-progress/LAB-018.md`
- `docs/reviews/LAB-018.md`
- FantasyWorldGenerator revision/status records only if the final behavior or protected-file hashes require them

Acceptance checks:
- focused city-layout tests pass;
- `scripts/ci/run-unit-tests.ps1` passes;
- `scripts/ci/validate-repo.ps1` passes;
- scoped adversarial review finds no unresolved blocker.

## Docs consulted

- `docs/README.md`, `docs/agent-workflow.md`, and `docs/implementation-status.md` for task workflow and truthful status reporting.
- `docs/doc-map.json`, routed via `python tools/docs_check.py --topic mathlab`.
- `docs/terrain-world-layers.md` and `docs/terrain-math-lab.md` for deterministic generation, strict validation, terrain constraints, and protected FantasyWorldGenerator scope.
- `docs/cicd.md` for required repository and unit-test commands.
- `board/templates/task.md` for evidence and review requirements.

## Documentation impact

No canonical behavior changed. The parser now accepts the already-authored profile schema, candidate indexing no longer crashes valid generation, and the helper retains its prior call compatibility. Existing `docs/terrain-math-lab.md` and `docs/implementation-status.md` claims remain accurate. The reviewed-byte hashes in `docs/mathlab-revisions.json` were refreshed for the current authorized FantasyWorldGenerator source/catalogue/doc state.

## Verification

Initial `scripts/ci/run-unit-tests.ps1`: failed with 21 errors. Twenty errors shared one building-pack validation exception (`religious_building profiles must be list of strings`); the focused layout test also called a helper whose new graph argument lacked a compatibility default.

- Focused `python -m unittest Sim.tests.test_terrain_city_layout -v`: 2 tests pass.
- `scripts/ci/run-unit-tests.ps1`: 112 simulation tests and 73 tooling tests pass.
- `scripts/ci/validate-repo.ps1`: passes; 77 original payload files, 129 protected files, 191 retired documents, 24 reviewed live FantasyWorldGenerator revisions/additions, and documentation links/map validate.
- `git diff --check`: passes; only expected Windows line-ending warnings are emitted for unrelated shared changes.
- Unreal verification is not applicable to this standalone Python/JSON FantasyWorldGenerator repair.

## Review and handoff

[Scoped adversarial self-review](../../docs/reviews/LAB-018.md) found and resolved the normalized-profile type confusion, candidate-list cursor overflow, and helper compatibility regression. No blocking finding remains. Unreal integration and visual/runtime acceptance remain outside this source-only repair.
