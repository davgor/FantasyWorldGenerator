# ML-01 — Portable contract, taxonomy, identity, units, and fixtures

## Requested behavior

Define the versioned portable contract that both the Python oracle and native kernel can implement without silent semantic mappings.

## Proposed mechanism

Add capability-description, taxonomy, identity, coordinate/unit, PRNG/order/numeric, schema-version, and failure contracts under `Contracts/`, plus independently authored conformance fixtures and invalid-input cases.

## Completion — 2026-09-16

ML-00 clean-checkout evidence is recorded in its completed ticket. ML-01a/b/c/d are complete for the scoped initial kernel: taxonomy, capability negotiation, coordinates/units, independent seed streams, numeric/canonical rules, persistent identity, versions and failures. The historical five-network vocabulary is superseded by the current eight-school recipe-3 contract.

## Acceptance and evidence

See the four completed slice tickets, canonical documents under `Contracts/`, and independently authored fixtures under `Fixtures/`. ML-02 exercises the identity/numeric/version contracts through a bounded transition. Full local validation passes: 213 reference tests and 55 facade/native-driver tests, plus provenance, lab smoke, showcase and asset determinism checks. Existing generated worlds and asset bytes are unchanged.

This closes the contract for the first supported rule, not every future entity or rule. New rules require explicit capabilities, versions and fixtures. ML-03 still needs complete native wire/numeric conformance and actual Unreal consumer evidence.
