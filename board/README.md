# MathLab ticket board

- `in-progress/` contains active work with recorded scope and evidence.
- `backlog/` contains reviewed, dependency-ordered work that is not yet active.
- `done/` contains imported historical LAB evidence and completed local tickets.

ML-00–03 have concrete local tickets. ML-04–13 remain roadmap epics in `PLAN.md` and must be decomposed into bounded behavioral slices before implementation.

HERO-GUILD’s opt-in planning calculator is complete. ML-00, ML-01 (including a/b/c/d), ML-02 and the scoped ML-03a headless C++ proof are complete. ML-03 is active. ML-03b native wire/numeric conformance and ML-03c isolated source packaging are complete, with full validation passing (276 tests). The Unreal runtime/package proof remains. Unreal 5.8.2 is the user-selected engine target and is not installed locally. The source uses a private owner-controlled distribution policy; shipping platform, engine CI and immutable artifact qualification are outstanding. See the active ticket for the next concrete steps.
