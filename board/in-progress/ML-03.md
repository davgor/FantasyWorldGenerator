# ML-03 — Native kernel and minimal Unreal runtime proof

## Requested behavior

Port the ML-02 transition into a portable native core, expose it through a generic runtime `.uplugin`, and qualify an immutable source-inclusive package in a clean minimal consumer.

## Proposed mechanism

Keep `Core/` free of Unreal types; wrap it in `Unreal/MathLabRuntime/`; use `Tests/UnrealConsumer/` for compile, load, runtime, serialization, and cooked execution. Compare native results with reviewed ML-01 fixtures.

## Dependencies and unresolved decisions

Depends on ML-02. Before scheduling, record plugin/module name, exact Unreal patch, compiler/toolchain, platform/architecture/targets, CI runner, artifact channel, compatibility manifest, and license. PK04 here means a cooked minimal consumer; the separate game retains its own integration gate.

## Acceptance and evidence

PK02, PK03, the minimal-consumer interpretation of PK04, PK08, and PK10. A Python wheel, JSON export, descriptor-only plugin, editor-only Python call, or mutable Actions artifact cannot satisfy this ticket.


## Current progress — 2026-09-16

ML-01 and ML-02 are complete. ML-03a's typed C++ transition passes the shared fixtures and local headless checks; see `board/done/ML-03a.md` and `Core/README.md`. ML-03b native JSON/candidate transport and numeric streams, and ML-03c reproducible source packaging with an isolated headless consumer, are complete. Full validation passes (213 reference +63 facade/native-driver tests); all nine native tests pass under UBSan. See their completed tickets for exact source/fixture and artifact evidence. These close the scoped headless work, not the Unreal or immutable hosted-release gates.

User-selected engine target: Unreal 5.8.2, not installed on this machine. Planned plugin/module name: `MathLabRuntime`, matching the existing proposed adapter path. Local headless compiler: Apple clang 17.0.0 / arm64 macOS; this is not a selected shipping platform or an Unreal-qualified toolchain.

Distribution policy: private owner-controlled source, root `LICENSE` and decision 017. This does not clear inherited third-party provenance or authorize public redistribution. No publication occurred. Shipping platform/architecture/targets, installed engine/toolchain, engine CI runner and immutable artifact channel remain unset.

Next integration step: implement and compile the generic `MathLabRuntime` adapter in a minimal Unreal consumer once a 5.8.2 engine runner and target platform are available. Editor/cooked qualification and an immutable runtime artifact channel remain mandatory; do not close PK02/03/04/08/10 based only on local Python/C++ proof.
