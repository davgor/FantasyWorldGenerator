# ANIM - Animation requirements

16 individually tracked production jobs. All start unchecked. Shared output/acceptance contracts are in [README](README.md); exact structured records are in [manifest.json](manifest.json).

Each checkbox means the complete job (source, derivatives and import verification), not merely an image or draft.


## ANIM-001 - Standing idle

- [ ] Produce and verify `010.ANIM-001`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Subtle breathing and weight shift. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-001/`.


## ANIM-002 - Walking loop

- [ ] Produce and verify `010.ANIM-002`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Grounded human-scale walk. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-002/`.


## ANIM-003 - Running loop

- [ ] Produce and verify `010.ANIM-003`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Readable run with stable foot plants. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-003/`.


## ANIM-004 - Slope locomotion test

- [ ] Produce and verify `010.ANIM-004`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Blend/retarget setup for local up and slopes. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-004/`.


## ANIM-005 - Turn in place

- [ ] Produce and verify `010.ANIM-005`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Left/right turning as one clip-set job. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-005/`.


## ANIM-006 - Sit and stand

- [ ] Produce and verify `010.ANIM-006`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Chair contact transition pair. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-006/`.


## ANIM-007 - Carry crate walk

- [ ] Produce and verify `010.ANIM-007`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Two-hand carrying loop. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-007/`.


## ANIM-008 - Push cart walk

- [ ] Produce and verify `010.ANIM-008`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Two-hand handle contact loop. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-008/`.


## ANIM-009 - Farm hoe action

- [ ] Produce and verify `010.ANIM-009`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Ground-contact farming action. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-009/`.


## ANIM-010 - Mine pick action

- [ ] Produce and verify `010.ANIM-010`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Pick impact against explicit work surface. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-010/`.


## ANIM-011 - Smith hammer action

- [ ] Produce and verify `010.ANIM-011`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Anvil contact action. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-011/`.


## ANIM-012 - Woodcut axe action

- [ ] Produce and verify `010.ANIM-012`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Controlled tool swing. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-012/`.


## ANIM-013 - Read book idle

- [ ] Produce and verify `010.ANIM-013`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Supported book and gaze pose. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-013/`.


## ANIM-014 - Guard idle

- [ ] Produce and verify `010.ANIM-014`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Alert neutral pose. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-014/`.


## ANIM-015 - Wizard study gesture

- [ ] Produce and verify `010.ANIM-015`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Original noncombat scholarly gesture. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-015/`.


## ANIM-016 - Sleep idle

- [ ] Produce and verify `010.ANIM-016`.

**Type / scale:** animation / Retarget to approved human/dwarf/elf proportions.

**Generate/assemble:** Rest pose on bed. Reuse existing compatible locomotion clips before commissioning replacements.

**Use:** Explicit authored/generated location or role; Place only from an explicit location/interaction record, never unconditional scatter.

**Dependencies:** PPL-001, PPL-011, PPL-021.

**Outputs and checks:** `animation` contract. Record source, licence, preview and validation evidence against this ID. **Destination:** `SourceArt/World/ANIM/ANIM-016/`.
