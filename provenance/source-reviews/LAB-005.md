# LAB-005 — scoped adversarial self-review

Scope: merge the user-authorized layered-world MathLab implementation against current main. Self-review, not an independent reviewer. Compared implementation with the lab recipe documentation, world compilation/integration boundary, preparation contracts and the accepted plan.

## Findings and resolutions

- Original port hashes rejected intended live changes. Added a bounded explicit revision record and validator; original manifests remain unchanged. Historical reference paths, wrong prior hashes, traversal, duplicate records, missing tickets and changed reviewed bytes are rejected. Added behavioral tests and included them in remote CI.
- Canonical docs still described an untouched 85-test lab and only three peoples. Updated index, status, world boundary, integration epic and routing to distinguish recipe 1 from historical snapshots and unimplemented Unreal capabilities.
- Merge isolation: copied only the task's 18 implementation/documentation files into a new worktree based on current origin/main. No engine configuration, binary assets, archive files or unrelated working changes were included.
- Regression review covers finite exports, named network isolation, sky geometry stability across population choices, exclusive fisheries, monthly food conservation and shared route capacity, conditional zones and explicit override precedence. Existing tests cover the legacy path.

## Evidence and limits

The implementation session passed 104 terrain tests and exercised Random/Parameters, the structured generation endpoint, seasonal/sky inspection and browser console checks. Merge validation reruns preservation/doc checks, both test suites and JavaScript syntax against the isolated branch, followed by a fresh exported Git-tree check and remote CI before merge.

Output remains an artistic lab model. Food/water quantities are proxies, high-resolution exports are large, and logistics is not an optimal shipping simulator. No Unreal editor, package, save importer, nests, moving tribes/circuses, Underdark or underwater settlement acceptance is claimed. These are documented scope limits, not silently supplied features.

Merge worktree results: repository preservation and documentation checks passed; 104 terrain tests and 19 tooling tests passed. JavaScript syntax passed. A fresh `git archive HEAD` export passed preservation, documentation and JavaScript syntax checks; this verifies committed bytes independently of the dirty source workspace.
