{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fantasy-world-generator.local/schemas/asset-list.schema.json",
  "title": "FantasyWorldGenerator exhaustive asset list",
  "type": "object",
  "required": [
    "schema",
    "schema_version",
    "scope",
    "content_sha256",
    "summary",
    "assets",
    "recipe_version"
  ],
  "properties": {
    "schema": {
      "const": "fantasy-world-generator.asset-list"
    },
    "schema_version": {
      "const": 2
    },
    "scope": {
      "type": "string",
      "minLength": 1
    },
    "content_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "summary": {
      "type": "object",
      "required": [
        "total",
        "by_kind",
        "by_source"
      ],
      "properties": {
        "total": {
          "type": "integer",
          "minimum": 1
        },
        "by_kind": {
          "type": "object",
          "additionalProperties": {
            "type": "integer",
            "minimum": 1
          }
        },
        "by_source": {
          "type": "object",
          "additionalProperties": {
            "type": "integer",
            "minimum": 1
          }
        }
      },
      "additionalProperties": false
    },
    "assets": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "kind",
          "name",
          "source",
          "status",
          "selectors",
          "metadata"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._-]+$"
          },
          "kind": {
            "type": "string",
            "minLength": 1
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "source": {
            "type": "string",
            "minLength": 1
          },
          "status": {
            "type": "string",
            "description": "Two vocabularies share this field and the compiler emits both. Simulation-derived rows carry a support claim: `supported` means a generated world can reach this identity, `unreachable` means the identity is kept so every world document and registry slot still resolves but no art is commissioned for it, and `schematic` means the identity is placed as a labelled footprint or marker with production art unassigned. Rows compiled from the production catalogue instead carry that catalogue's own work state, today only `not_started`. Adding a value here is a contract change; tests/test_asset_list.py::test_status_vocabulary_is_closed_and_matches_the_schema fails in both directions until the enum and the compiler agree.",
            "enum": [
              "supported",
              "unreachable",
              "schematic",
              "not_started"
            ]
          },
          "selectors": {
            "type": "object"
          },
          "metadata": {
            "type": "object"
          }
        },
        "additionalProperties": false
      }
    },
    "recipe_version": {
      "const": 3
    }
  },
  "additionalProperties": false
}
